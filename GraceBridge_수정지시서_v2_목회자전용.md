# [Claude Code 수정 지시서] GraceBridge v2 — 목회자 전용 전환

> 이 문서 전체를 기존 GraceBridge 프로젝트가 열려 있는 Claude Code에 붙여넣어 수정을 지시합니다.

---

## 0. 수정 배경과 대원칙

수익화 전략에 따라 플랫폼을 **목회자 전용 서비스**로 전환한다. 일반 성도는 당분간 가입 대상이 아니다.

**대원칙 — 반드시 지킬 것:**

1. **삭제가 아니라 숨김.** 성도용 기능(오늘의 묵상, 성경 Q&A)의 코드·DB 스키마·번역 파일은 삭제하지 말고 feature flag로 비활성화한다. 향후 "교회 단위 구독" 모델에서 재사용할 예정이다.
2. **설교 생성기는 건드리지 않는다.** 이번 수정에서 설교 생성 관련 코드(입력 폼, AI 호출, 결과 뷰어, 보관함)는 리팩토링하지 말 것. 회귀(regression) 방지가 최우선이다.
3. 모든 문구 수정은 **5개 언어(ko/en/fr/es/de) 번역 파일에 동시 반영**한다. 한국어만 고치고 끝내지 말 것.
4. 각 작업 단계 완료 시 `npm run build` 통과를 확인하고 커밋할 것.

## 1. Feature Flag 도입

```ts
// lib/features.ts (신규)
export const FEATURES = {
  MEMBER_SIGNUP: false,      // 성도 회원가입
  DEVOTIONAL: false,         // 오늘의 묵상
  BIBLE_QNA: false,          // 성경 Q&A
  CHURCH_CODE: false,        // 교회 코드 (향후 기능, 스캐폴딩만)
} as const;
```

- 위 플래그가 `false`인 기능은: ① 내비게이션/랜딩에서 노출 제거 ② 해당 라우트 접근 시 404 또는 랜딩으로 리다이렉트 ③ 관련 API Route도 403 반환.
- 환경변수로 오버라이드 가능하게 (`NEXT_PUBLIC_FEATURE_DEVOTIONAL=true` 등) — 개발·시연 시 켤 수 있도록.

## 2. 회원 체계 수정

- 회원가입 화면에서 역할 선택(목회자/일반 신자) UI 제거 → **목회자 가입 단일 플로우**로 변경.
  - 필수 입력: 이름, 이메일, 소속 교회명, 직분(담임/부목사/전도사/선교사/신학생/기타), 국가
  - 가입 즉시 `pastor_pending` 상태 → 관리자 승인 후 `pastor` 활성화 (기존 승인 절차 유지)
  - 신학생·선교사도 가입 가능하도록 직분 옵션에 포함 (잠재 고객층)
- 기존 DB의 `member` 역할 계정이 있다면 삭제하지 말고 그대로 두되, 로그인 시 "목회자 전용 서비스로 개편되었습니다" 안내 페이지로 이동.
- `admin` 역할과 관리자 페이지는 변경 없음.

## 3. 랜딩 페이지 전면 수정 (목회자 단일 타깃)

- 히어로 문구 교체 (한국어 원본, 나머지 4개 언어 자연스럽게 번역):
  - 배지: "목회자를 위한 AI 설교 동역자" (유지)
  - 헤드라인: "설교 준비의 짐을 덜고, 말씀에 더 깊이 머무르세요" (유지 가능)
  - 서브: "GraceBridge는 매주 설교와 성경 교육을 준비하는 목회자를 위한 다국어 사역 도구입니다." 로 교체 — "성도가 성경을 이해하도록" 문구 제거
- 기능 소개 섹션 재구성:
  - "목회자를 위한 기능 / 모든 성도를 위한 기능" **2단 구분을 없애고** 단일 섹션으로:
    1. AI 설교 작성기 (기존 유지)
    2. **목회 자료실** (아래 4번 참조)
    3. 내 설교 보관함
  - 오늘의 묵상, 성경 Q&A 카드는 노출 제거 (코드는 flag 처리)
- 하단에 신뢰 요소 추가: "본 플랫폼의 교육 자료는 이성규 목사가 직접 제작·검수합니다." (5개 언어)
- AI 면책 문구는 그대로 유지.

## 4. "성경 이해 라이브러리" → "목회 자료실" 전환

메뉴명·페이지 제목을 변경하고, 성격을 "성도가 열람하는 콘텐츠"에서 **"목회자가 사역 현장(성경공부·새가족 교육·소그룹)에서 사용하는 교육 자료"**로 재정의한다.

- 메뉴명: 목회 자료실 / Ministry Resources / Ressources pastorales / Recursos pastorales / Materialien für den Dienst
- 각 콘텐츠 카드에 **용도 태그** 추가: `설교 보조` `성경공부용` `새가족 교육` `소그룹 인도` `주일학교`
- 기존 `audience` 필드(목회자용/전체)는 용도 태그 체계로 마이그레이션 (DB 마이그레이션 스크립트 작성, 기존 데이터 보존)
- **다운로드·배포 기능 강화** (목회자가 실제로 쓰게 만드는 핵심):
  - 슬라이드: ① 화면 발표 모드(기존) ② PDF 다운로드(16:9 발표용) ③ **인쇄용 유인물 PDF**(A4, 슬라이드 2장/페이지 + 메모 칸)
  - 각 자료에 "이 자료 활용 팁" 필드 추가 (관리자가 입력: 예 - "새가족 교육 2주차에 사용 권장")
- 검색·필터를 용도 태그 기준으로 변경

## 5. 요금제 화면 (목회자 단일 구독)

결제 연동은 아직 하지 않되, 요금제 안내 페이지(`/pricing`)를 신설한다:

| 구분 | 무료 체험 | 스탠다드 (월) |
|---|---|---|
| AI 설교 생성 | 3회 (총) | 월 30회 |
| 목회 자료실 | 샘플 3개 | 전체 이용 + PDF 다운로드 |
| 설교 보관함 | 3편 | 무제한 |

- 가격 표기는 관리자가 나중에 정할 수 있도록 상수 파일로 분리 (`config/pricing.ts`), 화면에는 "출시 기념 가격 준비 중" 표기
- "구독하기" 버튼은 현재 "관리자 승인 요청" 폼으로 연결 (Stripe 연동 전 임시)
- 무료 체험 한도는 서버 측에서 실제로 카운트·차단 구현 (`usage_logs` 활용)

## 6. 향후 확장 대비 스캐폴딩 (구현은 최소, 구조만)

- `churches` 테이블 신설: (id, name, code, owner_pastor_id, created_at) — **UI는 만들지 않음**
- FEATURES.CHURCH_CODE = false 상태로, 목회자 프로필에 `church_id` 컬럼만 추가
- 주석으로 명시: "Phase 3 — 교회 단위 구독 시 성도가 교회 코드로 무료 접속하는 구조에 사용 예정"

## 7. 디자인 테마 전환 — 프로토타입 배색 적용

현재 사이트의 파란색·흰색 계열 테마를 프로토타입의 **"새벽 서재" 테마(따뜻한 종이 바탕 + 깊은 남빛 + 전례 금색)**로 전면 교체한다.

**색상 토큰 (Tailwind config 또는 CSS 변수로 정의하고, 하드코딩된 색상값을 전부 이 토큰으로 치환할 것):**

```css
:root {
  --ink:       #1D2440;  /* 본문 텍스트, 주요 버튼 배경 — 깊은 남빛 */
  --ink-soft:  #3A4266;  /* 보조 텍스트 */
  --paper:     #FBFAF6;  /* 카드·콘텐츠 배경 — 따뜻한 백지 */
  --paper-dim: #F1EFE7;  /* 페이지 바탕, 안내 박스 배경 */
  --gold:      #B8912E;  /* 포인트·강조·활성 상태 — 전례 금색 */
  --gold-soft: #E5D9B6;  /* 옅은 금색 — 호버, 테두리 강조 */
  --violet:    #6B5B95;  /* 절기 배지 등 보조 포인트 — 대림·사순 보라 */
  --line:      #DAD6C8;  /* 구분선, 카드 테두리 */
  --danger:    #A8493E;  /* 오류 */
}
```

**적용 규칙:**

- 페이지 바탕은 `--paper-dim`, 카드·콘텐츠 영역은 `--paper`. 순백(#FFFFFF)은 사용하지 않는다.
- 현재 사용 중인 파란색 계열(버튼, 배지, 링크, 로고 강조색 등)을 전부 교체한다: 주요 버튼은 `--ink` 배경 + `--paper` 글자, 강조 버튼·활성 탭·아이콘 포인트는 `--gold`, 절기/카테고리 배지는 `--violet` 테두리형.
- 히어로 영역 배경: `linear-gradient(180deg, #FBFAF6 0%, #F7F4EA 100%)`
- 로고 "GraceBridge"는 세리프체, "Bridge" 부분만 `--gold`.
- 제목·성경 구절에는 세리프체(**Gowun Batang**, Google Fonts) 적용, 본문은 기존 산세리프(Pretendard 계열) 유지. 라틴 문자 표시용 보조 세리프로 Cormorant Garamond 병기 가능.
- 노란 경고 박스(AI 면책 문구)는 `--paper-dim` 배경 + `--gold` 점선 테두리 스타일로 교체.
- 접근성: `--ink-soft` 이하의 옅은 색을 본문 텍스트에 쓰지 말 것. 주요 텍스트/배경 대비는 WCAG AA(4.5:1) 이상 유지 확인.
- 다크 모드가 구현되어 있다면 이번에는 라이트 테마만 교체하고, 다크 모드 팔레트는 별도 이슈로 남겨둘 것 (임의로 만들지 말 것).
- 5개 언어 모든 페이지에서 공통 적용 (테마는 언어와 무관하게 전역).

## 8. 작업 순서 및 확인

1. Feature flag 도입 + 성도 기능 숨김 → 빌드 확인 → 커밋
2. 회원가입 단일 플로우 전환 → 커밋
3. 랜딩 페이지 수정 (5개 언어) → 커밋
4. 목회 자료실 전환 + PDF/유인물 다운로드 → 커밋
5. 요금제 페이지 + 무료 체험 한도 → 커밋
6. 디자인 테마 전환(7번 섹션) — 색상 토큰 정의 후 전 페이지 일괄 치환 → 커밋
7. 전체 QA: ① 성도 관련 라우트 직접 접근 차단 확인 ② 5개 언어 전환 시 깨진 문구 없는지 확인 ③ 설교 생성 기존 동작 회귀 테스트 ④ 모바일(375px) 레이아웃 확인 ⑤ 파란색 잔재가 남은 화면이 없는지 전 페이지 육안 확인
8. 완료 보고: 변경 파일 목록, 스크린샷, 남은 이슈 정리

---

*수정 지시서 v2 / 2026-07-29 / 관리자: 이성규 목사 / 기획: 문형철*
